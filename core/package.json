{
    "name": "core",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "ganache-cli": "^6.12.1"
    },
    "scripts": {
        "test": "truffle test",
        "ganache": "ganache-cli --mnemonic $(grep MNEMONIC .env | cut -d '=' -f2)"
    },
    "dependencies": {
        "replace-in-file": "^6.1.0",
        "web3": "^1.3.1",
        "web3-abi-helper": "^4.2.3"
    }
}
